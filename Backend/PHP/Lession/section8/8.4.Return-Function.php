<?php
// Trả dữ liệu trong hàm
// 1.Trả về giá trị trong nội bộ hàm (thông qua lệnh hiển thị)
// $numbers = [0, 1, 2, 3, 4, 5];
// function show_array($data) {
//     echo "<pre>";
//     print_r($data); // In ra mảng $data
//     echo "<pre>";
// }
// show_array($numbers); // Gọi ra hàm với tham số -> thực thi câu lệnh in ra mảng

// 2.Trả về giá trị của hàm qua return
// $numbers = [0, 1, 2, 3, 4, 5];
// function show_array($data) {
//     return $data; // Trả về giá trị cho hàm show_array là $data
// }

// print_r(show_array($numbers)); // Muốn hiển thị ra giá trị của hàm phải dùng câu lệnh hiển thị

// -> return chỉ trả về giá trị cho hàm chứ không hiển thị;

?>